import React, { useEffect, useRef } from 'react';
import { PopunderConfig, AdPopunderProps } from './types';

const AdPopunder: React.FC<AdPopunderProps> = ({ config, onLoad, onError }) => {
  return null;
};

export default AdPopunder;
